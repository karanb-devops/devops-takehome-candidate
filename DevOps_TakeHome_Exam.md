# ข้อสอบภาคปฏิบัติ ตำแหน่ง DevOps

Lexnetix Co., Ltd.

ผู้สมัครที่ได้รับโจทย์นี้ผ่านการสัมภาษณ์รอบแรกแล้ว โจทย์ชุดนี้เป็นด่านสุดท้ายก่อนการสัมภาษณ์รอบสอง หากผลงานผ่านเกณฑ์ เราจะติดต่อนัดสัมภาษณ์รอบสองภายใน 3 วันทำการหลังได้รับงาน

**กำหนดส่ง** 5 วันนับจากวันที่ได้รับโจทย์
**เวลาที่ควรใช้** ประมาณ 4 ถึง 6 ชั่วโมง
**สภาพแวดล้อม** minikube บนเครื่องของผู้สมัคร
**การส่งงาน** อัปโหลดขึ้น GitHub แบบ public แล้วส่งลิงก์มาที่ devops-hiring@lexnetix.co.th

---

## เงื่อนไขการทำข้อสอบ

**การใช้ AI** อนุญาตให้ใช้เครื่องมือ AI ได้ทุกชนิดโดยไม่ต้องแจ้ง แต่ผู้สมัครต้องอธิบายทุกส่วนที่ส่งมาได้ ในรอบสัมภาษณ์เราจะเปิดไฟล์ที่ส่งมาแล้วถามเป็นจุด เช่น บรรทัดนี้ทำหน้าที่อะไร ถ้าลบออกจะเกิดอะไรขึ้น ทำไมเลือกวิธีนี้แทนวิธีอื่น ส่วนที่อธิบายไม่ได้จะไม่ถูกนับคะแนน

**repo เป็น public** ห้ามมี password, token, key, kubeconfig หรือข้อมูลจริงใดๆ ใน repo ให้ใช้ไฟล์ตัวอย่างที่มีค่าปลอมแทน และระบุใน README ว่าค่าจริงต้องใส่ที่ใด

**ไม่ต้องทำครบทุกข้อ** เราให้คะแนนที่คุณภาพมากกว่าจำนวนข้อ ข้อ 1 และ 2 เป็นข้อบังคับ ที่เหลือเลือกทำตามเวลาที่มี

**การแก้ปัญหาระหว่างทาง** เราไม่ได้ให้คู่มือติดตั้งหรือคำแนะนำแก้ปัญหาไว้โดยตั้งใจ ความสามารถในการหาทางออกเองเป็นส่วนหนึ่งของสิ่งที่เราประเมิน หากติดปัญหาที่แก้ไม่ได้ ให้บันทึกไว้ใน NOTES.md ว่าติดอะไร ลองวิธีใดไปแล้ว และประเมินว่าสาเหตุน่าจะมาจากอะไร บันทึกนี้มีผลต่อคะแนน

---

## ไฟล์ตั้งต้น

เราแนบ `devops-starter.zip` มาให้ ข้างในมีโครงโฟลเดอร์ ไฟล์ `.gitignore` แบบฟอร์ม NOTES.md โครงคำตอบใน `docs/` และไฟล์โจทย์ของข้อ 4

ให้ใช้โครงนี้เป็นจุดตั้งต้น แตกไฟล์ออกมา สร้าง repo แล้ว push ขึ้น GitHub จากตรงนั้น ไม่ต้องยึดโครงเป๊ะหากมีเหตุผลที่ดีกว่า แต่ให้เขียนบอกไว้ใน README

```
README.md            วิธีติดตั้งและรันตั้งแต่ต้นจนระบบทำงาน (เขียนทับของเดิม)
NOTES.md             ข้อจำกัด สิ่งที่ทำไม่ทัน และเหตุผลของการตัดสินใจ
docs/                คำตอบแต่ละข้อ เขียนเป็นไฟล์ .md
docs/images/         ภาพประกอบ
app/                 ซอร์สโค้ดแอปพลิเคชัน
k8s/                 manifest
Jenkinsfile          pipeline สำหรับข้อ 2
jenkins/             วิธีติดตั้ง Jenkins และไฟล์ประกอบ (ถ้ามี)
grafana/             dashboard และ config
troubleshooting/     ข้อ 4
```

**รูปแบบคำตอบ** ให้เขียนเป็นไฟล์ .md พร้อมภาพประกอบที่ยืนยันว่าระบบทำงานได้จริง เช่น ผลลัพธ์จาก terminal, หน้าเว็บที่เปิดได้, หน้า Grafana, หน้า GitHub Actions

ไม่ต้องแนบภาพหน้าจอของซอร์สโค้ด เพราะโค้ดอยู่ใน repo แล้ว ใต้ภาพแต่ละภาพให้ระบุหนึ่งบรรทัดว่าภาพนั้นยืนยันอะไร

---

## ข้อ 1 นำแอปพลิเคชันขึ้น minikube (30 คะแนน) บังคับ

เขียน HTTP service ขนาดเล็ก ภาษาใดก็ได้ ประกอบด้วย

| Endpoint | พฤติกรรมที่ต้องการ |
|---|---|
| `/` | แสดงชื่อ pod ที่ตอบกลับ และหมายเลขเวอร์ชันที่อ่านจาก environment variable |
| `/healthz` | ตอบ 200 เมื่อ process ยังทำงานปกติ |
| `/readyz` | ตอบ 503 ใน 15 วินาทีแรกหลังเริ่มทำงาน จากนั้นตอบ 200 |

นำขึ้น minikube โดยต้องมี Dockerfile, Deployment 2 replicas, Service, ConfigMap สำหรับหมายเลขเวอร์ชัน, liveness และ readiness probe, และการกำหนด resources requests กับ limits

**docs/part1.md ต้องมี**

- ภาพผลลัพธ์ `kubectl get pods` ที่แสดงสถานะ Running ทั้งสอง pod
- ภาพหน้าเว็บที่เข้าถึง `/` ได้ และเมื่อ refresh แล้วชื่อ pod เปลี่ยน
- คำตอบ: liveness กับ readiness probe ต่างกันอย่างไร และหาก readiness ไม่ผ่านแต่ liveness ผ่าน จะเกิดอะไรขึ้นกับ pod และกับ traffic
- คำตอบ: ค่า memory และ cpu ที่กำหนดใน resources มาจากการพิจารณาอะไร

---

## ข้อ 2 สร้าง CI pipeline ด้วย Jenkins (25 คะแนน) บังคับ

ติดตั้ง Jenkins บนเครื่องตัวเอง (container หรือติดตั้งตรงก็ได้) และเขียน `Jenkinsfile` แบบ declarative หรือ scripted ไว้ใน repo ประกอบด้วย stage อย่างน้อย

1. unit test อย่างน้อย 2 เคส
2. build image และกำหนด tag เป็น commit SHA
3. สแกน image ด้วย Trivy และให้ build fail เมื่อพบช่องโหว่ระดับ HIGH หรือ CRITICAL ที่มี fix แล้ว

ไม่จำเป็นต้อง deploy อัตโนมัติ

ข้อกำหนดเพิ่มเติม

- ห้ามมี credential อยู่ใน Jenkinsfile ให้ใช้ Jenkins credentials และอธิบายวิธีตั้งค่าใน README
- ระบุใน README ว่าติดตั้ง Jenkins อย่างไร ต้องลง plugin อะไร และสร้าง job แบบไหน เพื่อให้ทีมเรารันตามได้

**docs/part2.md ต้องมี**

- ภาพหน้า Jenkins ที่แสดง build ผ่านครบทุก stage (Stage View หรือ Pipeline Overview)
- ภาพ console output ของ stage ที่รัน Trivy
- คำตอบ: เหตุใดจึงไม่ควรใช้ tag `latest` เพียงอย่างเดียว
- คำตอบ: Jenkins ที่รันบนเครื่องตัวเองกับ Jenkins ขององค์กรที่ต้อง deploy ขึ้น cluster จริง ต่างกันตรงไหนในแง่ credential และสิทธิ์ และจะให้ Jenkins เข้าถึง cluster ด้วยวิธีใดโดยไม่วาง kubeconfig ที่มีสิทธิ์เต็มไว้บน agent

---

## ข้อ 3 Monitoring ด้วย Grafana (25 คะแนน)

ติดตั้ง Prometheus และ Grafana บน minikube แล้วสร้าง dashboard หนึ่งหน้าที่ทำให้ประเมินสถานะระบบได้ทันที

ขอบเขตประมาณ 3 ถึง 4 กราฟ ให้เลือกเองว่าจะแสดงอะไร โดยต้องอธิบายเหตุผลของการเลือกได้

export dashboard เป็น JSON เก็บไว้ที่ `grafana/dashboard.json` และระบุใน README ว่า import กลับอย่างไร

หากมีเวลาเหลือ ให้แอปพลิเคชันในข้อ 1 ส่ง metric ของตัวเองเพิ่มอย่างน้อย 1 ตัว แล้วนำมาแสดงบน dashboard ส่วนนี้เป็นคะแนนเพิ่ม

**docs/part3.md ต้องมี**

- ภาพ dashboard ที่มีข้อมูลจริง ไม่ใช่กราฟว่าง
- คำตอบ: หากเลือกได้เพียง 3 กราฟ จะเลือกอะไร ด้วยเหตุผลใด
- คำตอบ: หากตั้ง alert เข้า Slack ได้ 1 รายการ จะตั้งเรื่องอะไร ที่ค่าเท่าใด ระยะเวลาเท่าใดจึงยิง และเหตุใดจึงไม่ตั้งให้ไวกว่านั้น

---

## ข้อ 4 วิเคราะห์และแก้ไขระบบที่ทำงานผิดปกติ (20 คะแนน)

ไฟล์ `troubleshooting/broken-stack.yaml` ใน starter เมื่อ apply แล้วจะทำงานไม่ได้ มีจุดผิดหลายจุด ให้วิเคราะห์และแก้ไขให้ระบบทำงานได้ โดยตัวชี้วัดว่าแก้สำเร็จคือ pod ทุกตัวอยู่ในสถานะ Ready และเข้าถึงหน้าเว็บผ่าน Service ได้จริง

**docs/part4.md ต้องมี**

- อาการที่สังเกตได้
- ลำดับคำสั่งที่ใช้ตรวจสอบ พร้อมระบุว่าแต่ละคำสั่งใช้ดูข้อมูลอะไร
- จุดผิดที่พบ สาเหตุ และวิธีแก้ของแต่ละจุด
- ภาพ `kubectl get pods` ก่อนแก้และหลังแก้
- คำตอบ: จะป้องกันไม่ให้ความผิดพลาดลักษณะนี้หลุดขึ้น production ได้อย่างไร

เราประเมินที่กระบวนการวิเคราะห์เป็นหลัก มากกว่าจำนวนจุดผิดที่พบ

บันทึกไฟล์ที่แก้แล้วไว้ที่ `troubleshooting/fixed-stack.yaml`

---

## ข้อเลือกทำ (สูงสุด 10 คะแนน)

เลือกทำได้มากกว่าหนึ่งข้อ ทำได้กี่ข้อก็นับคะแนนสะสม แต่รวมแล้วไม่เกิน 10 คะแนน หากเวลาจำกัด แนะนำให้ทำข้อ 1 ถึง 4 ให้เรียบร้อยก่อน

- เขียนสคริปต์ตรวจสอบวันหมดอายุของ SSL certificate สำหรับหลายโดเมน
- ตั้ง Ingress ให้เข้าถึงแอปผ่านชื่อโดเมนแทนการใช้ port-forward
- ทำ configuration management ติดตั้งและตั้งค่า HAProxy หรือ nginx บน node หนึ่งตัว โดยต้องรันซ้ำได้แบบ idempotent จะใช้ Ansible, Terraform, Puppet, Chef, Salt, cloud-init หรือเครื่องมืออื่นที่ถนัดก็ได้ ขอให้ระบุใน README ว่าเลือกใช้อะไรและเพราะอะไร
- เขียนเอกสารออกแบบ 1 หน้า ว่าหากย้ายระบบทั้งหมดนี้ขึ้น GCP ส่วนใดใช้ได้เลย ส่วนใดต้องเปลี่ยน และข้อมูลส่วนบุคคลของผู้ใช้ในไทยต้องจัดการอย่างไรภายใต้ PDPA

---

## สัมภาษณ์รอบสอง

หากผลงานผ่านเกณฑ์ เราจะนัดสัมภาษณ์รอบสอง ระยะเวลา 1 ชั่วโมง ประกอบด้วยการนำเสนอสิ่งที่ทำ การตอบคำถามจากโค้ดที่ส่งมา และการแก้โจทย์สดบน repo ของผู้สมัคร กรุณาเตรียมเครื่องที่รันระบบนี้ได้มาด้วยและแชร์หน้าจอได้

หากมีข้อสงสัยเกี่ยวกับตัวโจทย์ ติดต่อได้ที่ devops-hiring@lexnetix.co.th
